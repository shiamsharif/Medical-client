# Browser requests stay on the frontend origin; Next.js proxies /api to this server-only target.
API_PROXY_TARGET=http://localhost:5000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Public sandbox key only; Stripe secret and webhook keys belong in the server .env.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
